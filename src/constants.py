ENDPOINT_URLS = {
    "web_app": {
        "base_url": "localhost:8000",
        "app_name": "app_webapp",
        "path": "/",
        "additional_paths": {
            "query": "/query"
        }
    }
}